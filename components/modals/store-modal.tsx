'use client';

import { Modal } from '@/components/ui/modal';

export const StoreModal = () => {
    return (
        <Modal title={''} description={''} isOpen={false} onClose={() => {}} />
    );
};
